name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  build:
    steps:
      - name: Print message
        run: echo "Hello, this is a simple text output!"

  deploy:
    runs-on: ubuntu-latest
